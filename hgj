#include<iostream.h>
#include<conio.h>
int main()
{
    int i,N,A[N];
 	 cin >> N;
 	 for(i=1;i<=N;i++)
 	     cin>>A[i];
    cout << endl;
    for(i=N;i>0;i--)
        cout<<A[i]<<endl;
    getch();
}
